define(function() {

    "use strict";

    var config = {
        
        game: {
            width: window.innerWidth,
            height: window.innerHeight
        }

    }

    return config;

});
