define(function() {

    "use strict";

    var config = {
        
        game: {
            width: window.innerWidth,
            height: window.innerHeigh
        }

    }

    return config;

});
