define(function() {

    "use strict";

    var config = {
        game: {
            width: window.innerWidth,
            height: window.innerHeight
        },
        movement: {
            speed: 5
        }
    }

    return config;

});
